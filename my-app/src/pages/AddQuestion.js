import React, { useState } from "react";
import { Modal } from "react-bootstrap";

const AddQuestion = (props) => {
  //   const [showModal, setShowModal] = useState(false);

  return <Modal>heyy </Modal>;
};

export default AddQuestion;
